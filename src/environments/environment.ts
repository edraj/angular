
window && ((<any>window)._indebug = true);

export const environment = {
    production: false,
    vercel: false,
    apiRoot: 'https://dmart.cc/dmart',
    localConfig: 'localdata/config.json'


};
